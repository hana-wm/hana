make cursor from xresources be used instead of default x11; hovering root window shows the normal xorg tiny cursor, while hovering over any other window it shows my custom cursor i configured. in dwm this isn't the case, so could it be something my wm's missing somewhere? can you please add it?

when i un-fullscreen a window, i want to avoid cursor stealing focus if it lands on any window other than the one i was fullscreened into. also, when i alt+tab (swap master and slave in stack) while focused on a master window, the master window gets moved to stack and focus is kept to that window, but if i swap slave to master, the cursor steals focus if it's located on top of another window. i don't want focus to be stolen in this case either. can you please place in the required guardrails to prevent these particular focus-stealing edge cases?

89shiroikuro:
make tiling layout set be a per-workspace configuration; if in a workspace i switch to monocle, other workspaces should remain on the default set tiling layout.
- Enable a setting in config.toml that converts workspace set to a global configuration setting, making it so switching to a different tiling layout switches it across all workspaces, just like how it currently works.

akaihanatsundeee:

here are the files.

also, i sent you this prompt already, but your thoughts got cut. i share you the transcript of all your thoughts. read it carefully as they contain really useful information for you, which will save you lots of thinking. it got cut abruptly, so please continue.

---

drag.zig
focus.zig
minimize.zig
workspaces.zig
utils.zig
master.zig
fibonacci.zig
grid.zig
monocle.zig

---

Now I have a complete picture of the entire codebase. Let me carefully analyze every operation that does visual changes and assess which ones need atomicity improvements.
Let me enumerate all the places where visual changes happen:
Already atomic (grab→commands→ungrab→flush in one batch):

fullscreen.enterFullscreen / exitFullscreen / toggleFullscreen — we just fixed this
workspaces.executeSwitch — uses defer _ = xcb.xcb_ungrab_server(wm.conn) + utils.flush at end. But wait, let me look more carefully...

I'm seeing a timing issue in executeSwitch: the server grab happens at the start, but the flush is called before the deferred ungrab actually runs, which means the ungrab command gets queued but never explicitly flushed before the function returns. There's a flush at the very end, but by then the defer has already executed the ungrab without flushing it first.
Wait, I need to reconsider the actual execution order in Zig...
Actually, I think I've been misunderstanding the timing. When utils.flush is called, it sends everything queued up to that point. Then bar.raiseBar() and bar.markDirty() happen, and only after the function scope exits does the defer block run with the ungrab. Since there's no explicit flush after the ungrab is queued, it sits in the buffer until the next event loop iteration flushes it. This means the server grab persists longer than intended—the ungrab won't actually reach the server until the next flush happens, keeping picom frozen during that window.
The real fix is to queue the ungrab before the final flush so that the grab, all the intermediate commands, and the ungrab all get sent to the server in a single write.
Now looking at workspaces.moveWindowTo, it's doing individual XCB calls without any grab protection. When it moves a window offscreen for the visual change, it just calls the configure and returns without flushing, relying on the flush happening later at the call site or event loop. For a single window move this might be acceptable, but it's not currently being flushed. I'm also noticing that window.zig's handleMapRequest doesn't use a grab either, though there's a comment explaining the reasoning about XCB's command ordering guarantees.
The real problem is in bar.setBarState — the map/unmap operations and the subsequent retile operations aren't atomic, so the bar visibility change and the workspace retiling can happen out of sync.
Looking at bar.retileAllWorkspaces, it's using a server grab to batch all the retiling operations together, flushing after each workspace to ensure the compositor sees the updates in order, then ungrabbing at the end to release everything atomically. I see the full flow now—we grab the server, then loop through each workspace calling retile and flushing inside the grab, which seems wasteful. After the loop we flush again, ungrab, and then synchronize with a get_input_focus round-trip before a final flush to ensure windows have their correct geometry before returning.
But wait, this defeats the purpose of grabbing in the first place—picom can see intermediate frames between each workspace retile. Let me check if retileCurrentWorkspace itself is calling flush, because that would explain the redundancy.
Checking the tiling module...
it doesn't call flush directly. So the explicit flush calls inside the loop are the problem. The ungrab gets queued after the last flush but only actually sent by the final utils.flush call, which is fine. Let me trace through this more carefully to understand the exact ordering.
The write buffer gets flushed at specific points: once per workspace in the loop, then again at the end. Since the grab is held during these intermediate flushes, picom can't composite frames anyway, so those flushes are actually safe. But I need to verify the ungrab ordering relative to the final flush. we flush multiple times inside the grab, picom can't composite until after the ungrab is processed, so the behavior is correct—just wasteful with unnecessary flushes. Now I'm realizing retileAllWorkspaces has a deeper issue: it temporarily swaps ws_state.current to iterate through workspaces, but functions like calculateScreenArea and retile read from that state and also check fullscreen status, which means the geometry cache gets modified for all workspaces through the same armGeomCache/disarmGeomCache calls.
Actually, looking closer, layouts.armGeomCache is called inside retile itself, so each workspace properly arms and disarms its own cache—that part checks out. Let me map out the actual problems: the .toggle case for bar.setBarState isn't atomic because the bar map/unmap happens in one flush, then retileAllWorkspaces runs in its own separate grab, so picom could composite between them and show the bar changed but windows still in old positions. For .hide_fullscreen and .show_fullscreen, the call originates from inside fullscreen.zig.
When bar.setBarState is called from within an existing grab, the flush inside it happens while we're already grabbed (which is fine—picom stays frozen), but then retileCurrentWorkspace queues more XCB commands that get buffered and flushed later by the outer function, which is actually correct. commands sit in the buffer until the outer utils.flush in exitFullscreen. That's fine.
But wait, there's a subtlety: setBarState calls utils.flush BEFORE tiling.retileCurrentWorkspace. So the sequence inside the grab is:

map/unmap bar
flush (bar change sent to server while grab is held - picom frozen, so ok)
tiling.retileCurrentWorkspace - queues configure_window calls
return to exitFullscreenCommit
more configure_window calls (border width, border pixel)
return to exitFullscreen
xcb_ungrab_server queued
utils.flush - sends retile + border + ungrab all at once

So picom sees: bar state change + retile + border restoration all at once. That's good.
But for bar.setBarState(.toggle), there's a different problem: the bar visibility flushes immediately, then retileAllWorkspaces runs with its own separate grab. Between those two events, picom can composite a frame showing the new bar state but with the old window positions, causing a visible flicker. The same issue happens in bar.toggleBarPosition where the bar position change and window property updates flush before the retile happens.
In workspaces.executeSwitch, the grab/ungrab pattern has a timing issue: the flush happens while the grab is active, but then bar.raiseBar queues more commands after the flush. When the function returns, the ungrab is deferred, so the sequence is flush → bar commands → ungrab, which means those bar commands execute after the flush but before the ungrab releases the server.
The fix would be to explicitly call bar.raiseBar before queuing the ungrab, then flush everything together. But actually, looking at the current behavior: the grab is sent first, all window commands are sent and flushed while the grab is held, so picom stays frozen during that entire operation, which might already be correct.
The real issue is that bar.raiseBar() and the ungrab end up queued but unflushed, meaning the server still has the grab active and picom remains frozen until the next event loop cycle. This adds latency but doesn't cause visual flicker since picom only composites once the grab is fully released. So it's functionally correct but slightly suboptimal for performance.
Now looking at minimize.minimizeWindow, which hides the window, refocuses, and updates the bar state...
Continuing with the minimize sequence...
the X server processes commands sequentially from the connection, and since everything gets sent in one write call, they're all queued up. But picom runs separately and can peek at the screen state between any two commands the X server executes. Without a grab to lock things down, picom could composite a frame after the window moves off-screen but before the retile completes—that's the flicker the user is concerned about.
For restore, the sequence is adding the window back, retiling the workspace, setting focus, then flushing.
The fullscreen path uses xcb_configure_window followed by a flush, then enters fullscreen mode which has its own grab built in. The non-fullscreen path skips the grab entirely, so there's a window where the tiling gets updated but the actual position change hasn't been sent to the X server yet.
When dragging starts, focus gets set and the window is removed from tiling (just marked dirty, no X calls), then the button press handler flushes afterward. During the drag itself, it's just moving one window at a time with a configure and flush—no grab needed there. When a window first appears, it goes through the setup tiling process.
For unmapping, the window gets removed from tiling and focus is cleared, then everything gets retiled if needed and flushed. This also has no grab, so there could be visual artifacts. Moving windows between workspaces involves various X calls to hide them offscreen or map them on the new workspace, but there's no explicit flush call in that sequence.
Now looking at bar.retileAllWorkspaces, it does grab the server but flushes inside the loop which is wasteful, and the ungrab sequence feels off. The real problem I'm seeing is that several operations change the bar state and then retile separately—the bar visibility change gets flushed before retiling happens, so the compositor sees the bar appear but windows still at their old sizes. Same issue with bar position changes. These need to be wrapped together in a single grab so the bar and window changes happen atomically. The minimize operation has a similar problem where it moves windows offscreen and then retiles without coordinating them. atomic. Fix: add grab.

minimize.restoreWindow (non-fullscreen) — addWindow + retile not atomic. Fix: add grab.
window.unmanageWindow — if window was fullscreen: setBarState + retile + focus changes all happen, but there's no grab. This could flash. Fix: add grab.
workspaces.executeSwitch — the defer pattern means ungrab is queued after the flush. The bar.raiseBar() happens after the flush too. So the final flush includes grab+commands+[nothing] and then bar raise + ungrab are queued but not immediately flushed. This means the grab holds a bit longer than needed. Not really a flicker but could cause the compositor to be frozen a bit too long. Fix: restructure to grab→commands→raiseBar→ungrab→flush.
bar.retileAllWorkspaces — the intermediate flushes inside the grab are wasteful. Harmless since picom is frozen during grab, but could be cleaned up.

Already correct: fullscreen.* just got fixed, workspaces.executeSwitch has grab coverage with minor defer inefficiency, drag.updateDrag doesn't need grab for single window moves, window.handleMapRequest relies on XCB ordering guarantees, and focus.setFocus batches intentionally without grab.
Now I'm planning the actual fixes, starting with bar.zig. The issue with setBarState is that for .toggle, the bar map/unmap and the retileAllWorkspaces call are separated by a flush, so they need to be wrapped together in a grab. But retileAllWorkspaces already has its own grab, and I'm not sure if nested grabs are safe in XCB—calling xcb_grab_server when already grabbed might be undefined behavior. The current structure has setBarState(.toggle) flushing the bar change then calling retileAllWorkspaces, which itself grabs and retiles.
The cleanest fix is to have setBarState queue the bar map/unmap without flushing, then call retileAllWorkspaces so both operations happen inside the same grab. Since retileAllWorkspaces already handles the grab, I just need to avoid the premature flush before calling it.
For the fullscreen cases where setBarState is called from within fullscreen.zig's grab, the flush sends data while the grab is held, which keeps picom frozen during the bar change. Refactoring the flush logic...
The cleanest approach is to defer the flush in setBarState so the bar's map/unmap operations get batched with the retile's grab, preventing picom from compositing an intermediate state. For the fullscreen cases, the outer grab's flush will handle everything anyway, so we just need to ensure side effects like the clock timer still trigger properly. the XCB write buffer, so when we flush, everything goes out in order to the X server—bar map, grab, retile commands, ungrab. The server processes them sequentially, which means the bar map happens before the grab takes effect, potentially letting picom composite it. So I need the bar change to happen inside the grab window instead. Either I grab first, then map/unmap the bar, then retile and ungrab all in one atomic sequence, or I accept a small race condition with the separate flush. The key insight is that a grab server command freezes input from other connections like picom, so the ordering of when commands are sent versus when they're processed matters.
If I queue the commands as map-then-grab-then-retile-then-ungrab, picom could composite between the map and grab. But if I reverse it to grab-first, then map and retile happen while picom is frozen, and only after ungrab does picom composite the final state—that's the flicker-free approach I need.
For setBarState, the solution is to queue a grab, then the bar visibility change, then call retile (which queues its own commands), then queue ungrab, and finally flush everything. The retile functions don't need to know they're inside a grab—they just queue commands like normal, and the outer grab wraps the whole operation atomically.
Implementing the grab wrapper... I'm realizing the nested grab situation is getting messy—I need to rethink this. The cleanest approach for toggling the bar state would be to queue the grab, handle the map/unmap and drawing, retile the current workspace without its own grab, and then carefully manage retilings across all workspaces when needed, but I'm hitting complexity around how to avoid nested grabs while keeping everything atomic.
Actually, the key insight is that only the current workspace needs to be retiled atomically with the bar visibility change—other workspaces are invisible anyway, so there's no visual flicker risk if we retile them separately. The current code retiles everything to avoid resize flicker when switching workspaces later, but that's an optimization, not a correctness requirement. So I should make the bar change and current workspace retile atomic within a single grab, then handle the other workspaces outside of it.
Let me redesign the toggle case...
Refactoring the code structure... I'm realizing those intermediate flush calls inside the grab are unnecessary since they're already serialized—I should just remove them and do a single flush at the end. For the toggle action, the clean approach is to grab the server, map or unmap the bar, redraw if needed, retile the current workspace, then retile the others outside the grab to avoid flicker, and finally flush everything.
For fullscreen state changes, since they're called from within an existing grab, I just need to queue the bar operations and retiling without creating a nested grab. The minimize operation follows a similar pattern—grab, hide the window, refocus, retile, then ungrab and flush—though refocus complicates things since setFocus intentionally doesn't flush.
For restoreWindow, it's straightforward: grab, add the window back, retile, focus, ungrab, flush. But unmanageWindow is more complex because of the fullscreen cleanup—if the window was fullscreen, I need to restore the bar, remove the window, retile if needed, clear focus, and refocus under the pointer. The tricky part is that the X server automatically removes the destroyed window, so I need to handle the tiling update without a visible gap.
I initially thought wrapping everything in a grab would prevent that flash, but there's a subtlety: focusWindowUnderPointer does a round-trip query for the pointer position. Inside a server grab, round-trips from our own connection still work fine—the server only freezes other clients. The real deadlock risk comes from passive grabs held by other clients, not from our own queries during an active grab.
Looking back at the fullscreen code's comment about needing the geometry query before the grab, I'm questioning whether that's actually necessary. A server grab doesn't affect pointer grabs from other clients in the way I initially thought—it just serializes protocol traffic. Let me reconsider what the XCB manual actually says about this. I see a real deadlock risk here: if another client's server grab request reaches the X server queue before ours, their grab freezes the server before our request is even processed, leaving us stuck waiting for a reply that will never come. The original caution makes sense—it's not about other clients' pointer grabs interfering, but about the server grab requests themselves getting serialized in a way that blocks us.
That said, the existing workspaces.executeSwitch code already does xcb_query_pointer inside a server grab and works fine in practice, so I'll follow that same pattern rather than overthinking this further.
Now looking at drag.startDrag—it calls focus.setFocus followed by tiling.removeWindow, which removes the window from tiling and marks things dirty. The focus call sends input focus and button grab changes, but none of this is wrapped in a server grab yet.
When a window is being dragged, it needs to be raised and focused, but I'm noticing that removeWindow only marks the tiling as dirty without actually triggering a retile. The retile would normally happen when retileIfDirty gets called, but during a drag operation there's no explicit retile call, so the remaining windows stay in their original positions until something else triggers the layout update.
This feels like a pre-existing issue that's separate from what I'm trying to fix, so I'll leave it alone for now.
Let me focus on the concrete changes needed: wrapping bar visibility toggles and position changes in single grab operations to prevent flicker, simplifying the grab pattern in retileAllWorkspaces by removing intermediate flushes, and wrapping the minimize operation's hide, refocus, and retile steps together in a grab.
For the restore path, I need to wrap the window addition, retile, and focus in a grab. The unmanage operation needs its visible state changes wrapped in a grab too. And in the workspace switch, I should fix the defer and flush ordering so the ungrab gets queued before the final flush.
Now I'm checking which files need these changes and thinking through the bar state logic—specifically whether the fullscreen hide/show paths need updates since they're already called from within the fullscreen module's grab, which should already protect them. But setBarState calls tiling.retileCurrentWorkspace at the end, and since we're inside a grab already, that should be fine.
For the hide case, the fullscreen state is set before setBarState is called, so the retile sees an active fullscreen and exits early. For the show case, the fullscreen state gets cleared first in exitFullscreenCommit, so the retile should also handle that correctly.
The issue is with the toggle case: setBarState flushes the bar map/unmap commands to the server before the grab completes, but then retileAllWorkspaces runs with its own separate grab. I need to restructure bar.zig to defer the flush so the bar and retile commands get batched together in the same grab. Writing the conditional logic...
Actually, I need to clarify the distinction between these two retiling functions. The private retileAllWorkspaces in bar.zig temporarily switches to each workspace, retiles it, then repositions non-current workspaces offscreen. Meanwhile, tiling.retileAllWorkspaces in tiling.zig is the public version that handles something slightly different. I'm trying to figure out which one should be called here and what the actual behavior difference is.
Looking at the logic for setBarState(.toggle), I want to split the work: retile the current workspace atomically with the bar visibility change inside the grab, then handle the other workspaces outside. Since tiling.retileAllWorkspaces already skips the current workspace by design, I can use that for the second step—but I need to make sure the current workspace gets retiled first in the grab.
Wait, there's a complication: bar.retileAllWorkspaces moves non-current workspace windows offscreen after retiling them to keep them hidden, but tiling.retileAllWorkspaces just retiles them in their correct positions without moving them offscreen. I need to check what tiling.retileAllWorkspaces actually does...
Looking at the code, it calls tileWithOffset which sends the proper tiled positions, not offscreen positions. So these windows would end up visible on screen even though they're on different workspaces. Actually, I think I'm getting confused about how X11 visibility works here—windows are always mapped, and they're hidden by positioning them at an offscreen coordinate, so if tiling.retileAllWorkspaces is sending visible tiled positions, it would be putting them back on screen when they should stay hidden.
Ah, I see what's happening now. The function iterates through each workspace, tiles it to on-screen positions, then for non-current workspaces it repositions those windows back offscreen using a negative X coordinate. I'm seeing that retileAllWorkspaces is supposed to retile inactive workspace windows so they have the correct size before the user switches to them, but I need to understand how those windows end up offscreen if the function is sending configure_window calls with tiled positions. Let me trace through what the layout functions actually do. Offsetcomputes positions starting from x=0, y=y_offset. These are VISIBLE positions. But the windows are currently at OFFSCREEN_X_POSITION (-4000 or similar). AftertileWithOffset` sends configure_window to x=0, the windows would appear on screen.
Hmm, but the comment says this is for workspaces the user ISN'T on. Wouldn't this make invisible workspaces visible?
Oh wait, I think I misread the comment. Let me re-read bar.retileAllWorkspaces more carefully. So the loop iterates through each workspace, and for inactive ones it temporarily retiles them to on-screen positions to update the geometry cache, then immediately moves the windows back offscreen before moving to the next workspace. offscreen
But with the grab active, picom never composites the intermediate state (on-screen with wrong layout), only the final state (offscreen). That's why the grab is needed.
But then tiling.retileAllWorkspaces (in tiling.zig) does:
zigswitch (s.layout) {
    .master    => master_layout.tileWithOffset(wm.conn, s, ws_windows, w, h, y),
    ...
This sends configure_window to the tiled (on-screen) positions for inactive workspace windows, without moving them back offscreen! Unless... hmm.
Wait, maybe I'm wrong. Let me look at tiling.retileAllWorkspaces more carefully.
So it's getting the current state, checking if tiling is enabled, calculating the screen area and workspace count. Then it's iterating through each workspace—skipping the current one since that's already handled elsewhere—and for fullscreen workspaces, the sibling windows are already offscreen so they'll get repositioned correctly when fullscreen exits.
For each workspace, I'm collecting all the tiled windows that belong to it into a buffer, then getting the screen dimensions and calculating the y-offset for positioning.
Now I'm realizing there's a potential issue: the code applies the layout tiling logic and updates borders for windows, but I'm concerned that this might be sending configure requests to windows on inactive workspaces, which could cause them to briefly flash on screen. The geometry cache should prevent this by skipping redundant configure calls, but since the cache was just armed at the start of this loop, it's likely empty or contains stale data, so the first configureSafe call for each window probably won't find a matching cached entry.
Let me look at the comment for tiling.retileAllWorkspaces more carefully to understand the actual intent — it seems the purpose is to pre-calculate and send geometry updates to offscreen windows on inactive workspaces so they're already positioned correctly before the user switches to them, avoiding resize flicker at workspace-switch time. The current workspace is intentionally skipped since it's already been retiled, and fullscreen workspaces are also skipped since their sibling windows are parked offscreen and will be retiled when exiting fullscreen.
But wait, there's something odd here — the comment says these windows should have the correct size, yet master_layout.tileWithOffset is sending them x and y coordinates based on the gap and offset, not the OFFSCREEN_X_POSITION where they should actually be. Let me trace where retileAllWorkspaces is actually being called from to understand the flow better. I'm realizing the public tiling.retileAllWorkspaces function might not be called anywhere in the codebase I've examined, or it could be invoked from a config reload handler or main loop I haven't seen yet. The real question is whether it actually handles inactive workspaces correctly.
Looking at the actual problem though: the current setBarState(.toggle) implementation queues the bar map/unmap, flushes to the server, then calls bar.retileAllWorkspaces which grabs the server again. I need to consolidate these operations into a single atomic sequence where the server grab encompasses both the bar state change and the retiling, so picom can composite everything together in one pass. flushes)
Picom can see bar change without retile.
Fix:

grab
Queue map/unmap bar + draw
Queue retile current workspace
ungrab
flush
Then retile other workspaces (bar.retileAllWorkspaces but skipping current ws — or just keep calling it as-is since we already retiled current ws, and the bar.retileAllWorkspaces will also retile current ws again but it should be a no-op via cache)

Actually, the simplest fix: just restructure setBarState so that for the toggle case, the bar map/unmap is inside retileAllWorkspaces's grab.
Actually, I realize the cleanest approach is: in setBarState, for the toggle case, don't flush before calling retileAllWorkspaces. Instead, flush inside retileAllWorkspaces. But retileAllWorkspaces grabs the server AFTER the flush would have happened. We need the grab to happen BEFORE the bar map/unmap.
So I need to restructure the toggle case to grab the server first, then queue the bar map/unmap and draw operations, then call retileAllWorkspaces which will add its commands and handle the ungrab and flush together.
I think the issue is that retileAllWorkspaces needs to be aware of whether the server is already grabbed. Or I need a variant of retileAllWorkspaces that doesn't grab. Let me go with the variant approach: create a helper that does the retile logic without grabbing, and have both setBarState and the normal retileAllWorkspaces call it.
Actually, simpler: just have setBarState(.toggle) grab, queue bar map/unmap + draw, then call a non-grabbing version of retile logic, then ungrab + flush.
I'm going to refactor retileAllWorkspaces to accept an optional parameter indicating whether to grab, or split it into two functions: one that grabs and one that doesn't.
I think the cleanest is to have retileAllWorkspaces always grab (as it does now), but have setBarState NOT call it directly for the toggle case. Instead, setBarState will do its own grab, queue the bar change, and then call a helper function that does the retile without grabbing.
So the flow for setBarState(.toggle) becomes:

Grab server
Map/unmap bar window
Draw bar
Call retile helper (no grab)
Ungrab and flush

And retileAllWorkspaces stays as-is for other callers.
I need to extract the retile logic into a separate function that doesn't grab, then have retileAllWorkspaces call it after grabbing.
I'm realizing the real issue: I need to ensure the bar map/unmap happens atomically with the retile, all within a single grab. The cleanest way is to have setBarState handle the grab for the toggle case, queue the bar change, then call the retile logic (without its own grab), then ungrab.
So I'll refactor to have a retileAllWorkspacesNoGrab function that does the actual retile work, and have retileAllWorkspaces call it after grabbing. Then setBarState(.toggle) can grab, queue bar change, call retileAllWorkspacesNoGrab, then ungrab.
I think I need to step back and just implement this: split the retile logic into a no-grab version, use it in both places, and let setBarState manage the grab for the toggle case.
I see the real solution now: I need to refactor so that the retile logic is separate from the grab/flush logic. Then setBarState can grab, queue bar change, call the retile logic, and ungrab/flush all in one atomic operation.
Let me just implement a retileAllWorkspacesNoGrab function and use it appropriately. I see the issue now—if I remove the flush from setBarState and let retileAllWorkspaces handle it, the bar change would be queued but not flushed until after the server grab happens. The problem is that retileAllWorkspaces grabs the server first, then flushes, so the bar visibility change would already be in the queue when the grab takes effect. The real issue is that picom can composite between the bar changes and the retile, so I need to reorder the commands: grab first, then bar map/unmap, then retile, then ungrab. But retileAllWorkspaces already has its own grab/ungrab logic, which would create nested grabs if I add another one at the setBarState level. I need to either remove the grab from retileAllWorkspaces when called from the toggle case, or restructure how the grab management works.
The cleanest solution is to make retileAllWorkspaces not grab by default, and let setBarState handle the grab for the toggle case. For the fullscreen cases that already run inside a grab, the bar change and retile happen within that existing grab context.
Actually, I'm realizing I might be overcomplicating this. The user only sees the current workspace, so I only need to keep the bar change and current workspace retile atomic inside the grab. The other workspaces can retile afterwards without any visual flicker, and when the user switches workspaces, executeSwitch will handle positioning anyway.
So the cleaner approach is to restructure setBarState to wrap the critical operations in a single grab, then call retileAllWorkspaces afterwards to handle the invisible workspaces—though that means retiling current twice, which should be mostly harmless with the geometry cache.
Refactoring the code... I'm finishing up the function by unmapping the window when needed, flushing the connection, and then retiling just the current workspace instead of all workspaces. After that I'm logging the bar state change and updating the clock timer. I'm stepping back now to reconsider the higher-level approach to this atomicity problem.
The real insight is that a server grab only matters when the screen goes through multiple intermediate visual states that would look wrong if the compositor tried to render them. A single XCB call is already atomic, so I need to be selective about which operations actually benefit from a grab. Looking at the operations in the window manager, I can see that bar toggles, bar position changes, window minimize/unminimize, and window destruction all involve multi-step visible transitions that would benefit from a grab, while workspace switches and fullscreen changes already have grabs in place.
For the operations that don't need grabs—like drag updates, focus changes, and window moves—these are either single-step operations or already rely on XCB's in-order guarantee, so they don't need the extra synchronization overhead. Some operations like bar.retileAllWorkspaces already have grabs but have unnecessary intermediate flushes that could be cleaned up.
Now I'm looking at drag.startDrag more carefully—it calls removeWindow which marks the workspace dirty, but there's no explicit retile call afterward. The remaining windows would stay in their old positions until the next event triggers a retile, which seems like a separate bug. I need to trace where retileIfDirty actually gets called in the main loop to understand if this is handled implicitly or if it's genuinely missing.
For the atomicity fixes the user asked for, I'm identifying the key operations that need to be wrapped: toggling bar state with retile, moving the bar with retile, cleaning up the workspace retile function, hiding and refocusing windows with retile, restoring windows with retile and focus, and wrapping the unmanage operation. Each of these needs the grab pattern to ensure the changes happen atomically.
For unmanageWindow specifically, I'm realizing the window is already gone from the X server when this is called, so there's no intermediate composite state picom needs to worry about—the retile just repositions the remaining windows. The tricky part is focusWindowUnderPointer doing a round-trip, but that's safe from within our server grab since we're not waiting for input events that another client's grab would block.
There's a complication though: if the window was fullscreen, bar.setBarState(.show_fullscreen) might trigger a retile, and then tiling.retileIfDirty could do another one. I need to make sure all of this happens within a single grab to keep the state consistent.
Tracing through the unmanageWindow logic...
Continuing with the rest of the function... Writing the unmanage function... I'm noticing that bar.setBarState(.show_fullscreen) already handles retiling when the window was fullscreen, so calling tiling.retileIfDirty afterward could trigger a second retile if the dirty flag was set by removeWindow — though the geometry cache should make the second pass mostly redundant.
For non-fullscreen workspaces, setBarState might return early if the bar visibility hasn't changed, which means retileIfDirty would be responsible for handling the actual retile instead. anything. Good.
For .show_fullscreen on a workspace where the bar was hidden: it would show the bar and retile.
OK so the logic is:

If was fullscreen: setBarState(.show_fullscreen) shows bar + retiles. retileIfDirty might be a no-op.
If not fullscreen: setBarState(.show_fullscreen) is a no-op. retileIfDirty retiles.

But wait, for the non-fullscreen case, setBarState is still called (it's a no-op). That's fine. retileIfDirty then handles the retile.
There's another subtlety: retileIfDirty only retiles if dirty == true. removeWindow calls s.markDirty(). So after removing a window, dirty is true and retileIfDirty will retile. Good.
But here's the issue: retileIfDirty is only called when the destroyed window was focused. If an unfocused window gets destroyed, the retile never happens even though the dirty flag is set. Looking at the code, retiling only occurs inside the was_focused branch, so destroying an unfocused window leaves the layout in a stale state. That seems like a potential bug.
Actually, let me check if there's another retile path. The dirty flag gets set by removeWindow, so maybe some other part of the event loop calls retileIfDirty periodically, or maybe the next focus change triggers a retile. Let me look at what retile functions are actually exported from the tiling module. That's getting into scope creep—the user asked for atomicity, not fixing other potential issues. The drag behavior where remaining windows fill space immediately is a design choice that varies across window managers anyway. Let me focus on what was actually requested and finalize which files need changes.
I need to wrap the bar state toggle and position changes in a grab, clean up the retile function to make the grab optional, do the same for minimize and restore operations, handle window unmanagement atomically, and fix the defer and flush ordering in workspace switching. I should also add that geometry cache invalidation function to tiling.
Now I'm working through the specific changes needed for setBarState(.toggle) and how the flow should work...
The key insight is that I need to grab first, then map or unmap the bar with its drawing, queue all the retiling commands without an inner grab, then ungrab and flush everything at once.
Since retileAllWorkspaces is only called from setBarState, I can refactor it into a commit-only version that just queues commands without managing the grab itself.
For the toggle case, I'm wrapping the bar state change and retiling inside a single server grab, while the non-toggle case just flushes after the bar change and retiles the current workspace separately.
Defining the commit version... I'm iterating through each workspace, retiling it if it has windows and isn't in fullscreen mode, then moving windows off-screen for workspaces that aren't currently active to avoid visual artifacts. After processing all workspaces, I'm restoring the original workspace state and skipping the synchronization barrier since the caller will handle flushing all changes at once.
Now I'm reconsidering whether that synchronization barrier from the original retileAllWorkspaces was actually necessary—it was there to ensure windows had correct geometry before returning, but inside a server grab the commands haven't been processed yet anyway, so the sync is pointless. For executeSwitch, the round-trip to query the pointer works fine inside the grab since it's just our connection querying the server.
Looking at toggleBarPosition...
Refactoring the function...
I'm wrapping the window configuration in a server grab to prevent race conditions, then checking if the workspace is fullscreen before retiling—but I'm realizing that setWindowProperties makes round-trip calls through getAtom, which could be problematic while the server is grabbed. inside grabs are fine for our connection. But they might be slow. Let me check if the atoms are cached...
setWindowProperties calls utils.getAtom(conn, "_NET_WM_STRUT_PARTIAL"), utils.getAtom(conn, "_NET_WM_WINDOW_TYPE"), etc. These are NOT cached (they go through utils.getAtom, not utils.getAtomCached). So they do round-trips.
These round-trips inside our grab are fine (our connection is not frozen). But it's slightly inefficient. For correctness though, we want the bar position to change atomically with the retile.
Actually, the property changes don't affect the visual rendering directly - they're metadata for the compositor. What matters visually is the configure_window(Y) and the retile. The property changes can happen before or after without any flicker concern. But I think it's more efficient to do the property updates first outside the grab since they involve round-trips, then grab just for the atomic bar position and retile operation.
Writing the function... I'm realizing the order matters here—setting window properties first with its implicit flushes happens outside the grab, then I queue the server grab, the Y position configure, the retiling operations, and finally ungrab, sending everything in one batch at the end. This way Picom sees the bar movement and retiling as atomic. Now I'm moving on to think through the minimize logic.
For minimizeWindow, the sequence should be: grab the server, hide the window with a configure call, refocus, retile the workspace, then ungrab and flush everything together. When minimizing a non-fullscreen window, setBarState(.show_fullscreen) is a no-op since the bar is already visible, so it just returns early before calling retile and flush.
Wrapping the XCB operations...
Actually, I need to check whether setBarState(.show_fullscreen) calls utils.flush internally—it does, which means the flush happens while the grab is still held, keeping picom frozen during the bar update and retiling.
For the non-fullscreen path in restoreWindow, I'm wrapping the window addition, retiling, and focus setting in a server grab so all those queued commands execute atomically before picom unfreezes.
For unmanageWindow, the critical piece is ensuring that when a tiled window gets destroyed, the remaining windows retile without picom catching an intermediate state where one window is gone but the layout hasn't updated yet. I'm handling fullscreen cleanup first...
Continuing with unmanage logic... it inside the grab. For the fullscreen case, bar.setBarState(.show_fullscreen) needs to happen inside the grab too, but we cleared the fullscreen state before grabbing the server. Actually, that works out—when setBarState checks if fullscreen is active, it'll see nothing since we already cleared it, so it'll show the bar and retile correctly. The sequence is: clear fullscreen state before the grab, then inside the grab, if we were fullscreen, call setBarState to show the bar and retile.
Now I'm writing out the actual implementation—defining the unmanageWindow function that handles removing a window from the tiling system, clearing its focus state if needed, and managing the fullscreen state transitions. I'm noticing the original code has retileIfDirty gated behind was_focused, which means non-focused windows don't trigger an immediate retile when destroyed. I need to preserve this behavior rather than calling it unconditionally, even though removeWindow marks things dirty—this gating might be intentional to let the event loop handle it instead. I'm realizing the issue—when both was_fullscreen and was_focused are true, setBarState already retiles internally, so I'd be retiling twice if I also call retileIfDirty in the focused block. I need to add a guard to skip the explicit retile when fullscreen was just cleared. iles, and then... we don't call retileIfDirty explicitly for the fullscreen case. Let me keep it simple:
Actually, bar.setBarState(.show_fullscreen) calls tiling.retileCurrentWorkspace internally (not retileIfDirty). And retileCurrentWorkspace calls s.clearDirty(). So after setBarState, dirty is cleared. Then retileIfDirty would be a no-op. So there's no double-retile issue.
I'm setting up the server grab, calling setBarState to show the fullscreen bar and retile if needed, then conditionally retiling again only if tiling is enabled and the window was focused. After that, I'm ungrabbing the server, flushing the connection, and marking the bar dirty. The nice thing about setBarState is it's safe to call even when the bar is already visible—it just returns early without doing anything, so there's no wasted work.
Now for workspaces.executeSwitch, I need to fix the defer ordering. The issue is that xcb_ungrab_server is deferred to scope exit, but utils.flush is called before the bar operations queue their requests. So the ungrab happens after flush, which means the bar operations aren't included in the flushed batch.
The fix is to explicitly call ungrab right after the bar operations and before flush, so everything gets flushed together and the server grab is released immediately rather than staying locked until the next event loop flush.
Now I'm thinking about the drag behavior—when a window starts dragging, should the remaining windows retile to fill the gap? The comment suggests they should, so I'll add a retile call right after removing the window from tiling.
Writing the drag retile logic... I see that the flush does happen after startDrag returns, so if I move the grab, retile, and ungrab into startDrag, they'll all be batched together atomically before the flush. The focus.setFocus call also happens inside startDrag, which means the window raise and input focus will be coordinated with the retiling in the right sequence. + focus change + retile of remaining windows all happen together. The most important visual thing is that the remaining windows retile (since the dragged window is being actively manipulated). The focus change is subtle.
For simplicity, let me just wrap the grab around focus.setFocus call too. But actually we need to be careful about the isWindowMapped check in setFocus (it does a round-trip). For .user_command, the check still runs since both conditions are true, so this would add an extra round-trip to the server.
The cleanest approach is to grab the server first, then call setFocus to raise and focus the dragged window, then remove it from the tiling layout and retile the remaining windows, all within the grab so these operations happen atomically. For moveWindowTo, I need to handle two cases: if the window is leaving the current workspace, I hide it offscreen and clear focus if it was focused, then mark tiling as dirty. If it's arriving in the current workspace, I map it and mark tiling dirty.
Looking at where this function gets called, I notice that moveWindowTo sets the dirty flag but doesn't actually trigger a retile—that happens elsewhere. When moving a window via the .move_to_workspace action, the current workspace should retile to fill the gap left behind, but there's no explicit retile call after moveWindowTo completes, which seems like it might be a lingering issue.
I'll add an atomic retile for the move_to_workspace action to ensure consistency with the other operations I'm making atomic. Now I have enough context to start implementing—I need to modify bar.zig to handle the state changes and retiling for bar operations. 2. minimize.zig - minimizeWindow, restoreWindow
3. window.zig - unmanageWindow
4. workspaces.zig - executeSwitch, moveWindowTo
5. tiling.zig - (already has invalidateGeomCache, no further changes needed)
6. drag.zig - startDrag
Actually, I need to make sure tiling.zig has the invalidateGeomCache fix since the uploaded files are the originals. And for moveWindowTo in workspaces.zig, I'm realizing there's a stale layout issue — when moving a window away from the current workspace, I need to retile immediately so the remaining windows fill the gap. I'll add a retile call under a grab to ensure picom sees the complete transition without flushing, since callers handle that.
Writing workspace transition logic... Wrapping the visual updates in a server grab to ensure the window configuration, focus clearing, and tiling recalculation happen atomically when moving from the current workspace. Handling the fullscreen teardown logic...
So when moving a window from the current workspace, if it's fullscreen I need to clear that state and trigger a bar update which retiles everything. Then I move the window offscreen and mark the tiling as dirty for the next update. The key thing is the window gets removed from the workspace during the move, so when dirty retiling happens it won't be included anyway—I'm just ensuring the tiling state stays consistent.
Let me trace through what actually happens step by step when moving window B to verify the retiling behavior is correct. The remaining windows retile correctly after B is excluded. B itself needs to be moved offscreen explicitly since the retile doesn't touch it—it's no longer on the current workspace. The subsequent markDirty() call is redundant since the retile already ran with B excluded, but it doesn't hurt anything. So the existing logic checks out.
For moveWindowTo, I'm wrapping the visual operations in a grab to make them atomic. When moving from the current workspace, I need to handle fullscreen teardown with setBarState if needed, then hide the window, clear focus, and retile if it wasn't fullscreen—all within the grab. When moving to the current workspace, I grab, map the window, mark dirty, retile, then ungrab. I notice the non-fullscreen case from the current workspace calls markDirty() but doesn't retile, which means the user's atomicity request suggests I should add retileCurrentWorkspace here to keep the hide and retile together. Let me add that for consistency.
Actually, adding an immediate retile to the non-fullscreen case would change the existing lazy-retile behavior, which could break something. I should be conservative and just wrap the existing XCB calls in a grab without adding new retile logic. For the non-fullscreen case moving from the current workspace, that's just a single configure call, so the grab doesn't add much value there. Let me reconsider whether this approach is even worth it.
The fullscreen case is different — bar visibility changes plus retile plus window hide need to be atomic, so a grab actually matters there. But for the simple cases like mapping a window or configuring it offscreen, a single XCB call is already atomic by protocol. So I should only add a grab where it genuinely helps: the fullscreen teardown scenario. For the other operations like toggling the bar or minimizing, those are the places where multiple XCB steps happen and a grab would prevent visual glitches. Now I'm mapping out the specific files that need changes: wrapping state transitions in grabs across bar.zig, minimize.zig, window.zig, and workspaces.zig, plus fixing the defer pattern in executeSwitch to queue the bar raise before ungrabbing, and adding cache invalidation to tiling.zig. I need to trace through the grab sequencing in minimizeWindow more carefully—specifically whether hideWindow should happen before or after the grab starts, since if it's before the grab, the window gets hidden and composited by picom before we've locked the server.
The solution is to grab the server first, then hide the window and retile inside the grab, but there's a timing issue: trackMinimized needs to record the window as minimized before the retile happens so t
